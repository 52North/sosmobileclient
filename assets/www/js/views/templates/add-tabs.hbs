<div class="pill tabs" id="add-tabs" gumby-fixed="top" style="z-index: 9;">
  <ul class="tab-nav">
    {{#each tabs}}
      <li class="{{active}}"><a href="#browser">{{name}}</a></li> <!-- class= active -->
    {{/each}}
  </ul>
    {{#each tabs}}
      <div class="tab-content {{active}} {{content-class}}" id="{{id}}"></div> <!-- class= active -->
    {{/each}}
</div>